# Base config file for experiment settings
# Path: crbr/config_templates/experiment.yaml

project_name: CMB/3Dunet            # Top-level ClearML organizational/dir name (can also be set with --project-name)
task_name: valdo1-train      # Optional name for the run (usually set from cmd with -t / --task-name)
task_description: Train 3D Unet on VALDO1  # Optional text description of the experiment (can also be set with --task-description)
