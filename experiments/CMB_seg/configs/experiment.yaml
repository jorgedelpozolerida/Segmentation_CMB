# Base config file for experiment settings
# Path: crbr/config_templates/experiment.yaml

project_name: CMB/testing            # Top-level ClearML organizational/dir name (can also be set with --project-name)
task_name: valdo-test-deepunet       # Optional name for the run (usually set from cmd with -t / --task-name)
task_description: Test demo   # Optional text description of the experiment (can also be set with --task-description)
